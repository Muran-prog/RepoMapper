# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
*.swp
*.swo
*~

# Editors
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local tooling (none used by Cart itself, but reserved for future add-ons)
node_modules/
.parcel-cache/
.cache/
dist/
build/

# Local data — e.g. self-hosted PMTiles archives
*.pmtiles
*.mbtiles
data/

# tools/ working directory (raw DEM / GeoTIFF / mbtiles artefacts produced
# by the build-*.sh scripts). The final PMTiles output is also git-ignored
# above; this just keeps the working tree clean.
tools/.work/
tools/planetiler.jar

# Environment overrides
.env
.env.local
